## deps 

    - fedora deps
        
        sudo dnf install gcc make libX11-devel libXft-devel libXinerama-devel patch fontconfig-devel -y

    - arch deps 
        sudo pacman -S base-devel git libx11 libxft libxinerama freetype2 fontconfig
